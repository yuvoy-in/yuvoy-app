# The contract this app is generated against.
#
# Pinned to yuvoy-api master. PR #50 (feat/m18-hardening) merged 2026-08-20,
# and master has moved on since: traveller OTP sign-in was DELETED
# ("the verifying bound is internal, and the second sign-in is deleted"), so
# /auth/otp/* and /me are gone. /me/bookings survives and is authenticated by
# the status token the recovery OTP returns — recovery IS the sign-in.
#
# `pnpm contract:check` fails the build if this copy stops matching the ref.
repo=yuvoy-in/yuvoy-api
# 9 Sep 2026. Forward from 1cbffaa07230, across six deploys in one day
# (#130, #131, #133, #135, #136, #137). PURELY ADDITIVE to everything this app
# reads; the only removals are two `/experiences` query parameters the API had
# never implemented.
#
# Consumed in this change:
#   - `/search` honours a filter with an EMPTY `q` (yuvoy-api#132 → #133). It
#     used to return before reading `destinationKey` or `bookableOn`, so the
#     day chips this app has shipped for a month answered "Nothing on that day"
#     against real departures. Text and filters are independent now; either
#     alone is a real search and together they intersect.
#   - `/search` gains `category` (closed enum, unknown = 400) and
#     `activityType` (open set, unknown = empty page). The asymmetry is
#     deliberate and both are handled: a 400 is a bug in this client, an empty
#     page is a vocabulary that grew since the last deploy.
#   - `Scan.destinationKey` (yuvoy-app#27) — a printed card says where it is.
#     Absent for a market-wide card AND for an unknown code, which is one shape
#     for "no destination" rather than two.
#   - `Attribution.source` gains `web` (yuvoy-api#134 → #135) — our own
#     marketing site, which is neither `referral` (somebody else's) nor
#     `direct` (typed the address).
#   - `POST /operator-applications` gains `privacyAccepted`, `marketingOptIn`
#     and `utm` (yuvoy-web#157). Consumed by yuvoy-web, not by this app.
#
# NOT consumed, and deliberately:
#   - `/experiences` now declares `destinationKey` and `bookableOn`. This app
#     calls that endpoint nowhere — the feed reads `/reels` and search reads
#     `/search` — so the declaration changes nothing here. Its `mode` and `q`
#     were REMOVED; `git grep` confirms neither was ever sent from this repo.
#
# `git ls-remote` was the check for this ref, not `gh api .../commits/master`,
# which served a five-minute-stale SHA twice on 8 Sep and cost a retracted
# issue. And every field above is consumed as OPTIONAL regardless of what
# `required:` says — a pinned contract states what an API WILL send, never what
# it does send today (yuvoy-api#126).
#
# ---
#
# 10 Sep 2026, second bump today. Forward from 3a643cde69c0 to 64d49ee5481e.
# Three new paths and three new schemas; nothing removed.
#
# Consumed in this change:
#   - `POST /reservations/{id}/cash-booking` + `CashBooking` (app#29). The
#     traveller commits in the app and pays the operator in cash on the day.
#     This is currently the ONLY way a booking can be completed at all: the
#     payment step reached `coming_soon`, rendered the message and stopped, and
#     the held seats lapsed fifteen minutes later.
#   - `payAtCounter` on the payment-order answer. Branched on by PRESENCE, not
#     by `state` — see the note below, which is the one place this bump is
#     ahead of the document.
#   - `GET /operators/{slug}` + `OperatorProfile`, and
#     `GET /operators/{slug}/reels` + `ReelPage` (app#30). A reel opened its
#     listing and the operator on that listing opened nothing.
#
# NOT consumed here, and it needs the API to move first:
#   - The LINK to an operator page from a reel or a listing card.
#     `OperatorSummary` still carries `id`, `name`, `verified`, `logoUrl` and
#     `credentialsSummary` — no `slug` — so a card has nothing to link with.
#     Confirmed against the live API as well as the document. Raised on
#     yuvoy-app#30; the page ships now and the links land when the field does.
#
# ## Where this repo is deliberately ahead of the contract
#
# `payAtCounter` is declared ONLY on the `200 coming_soon` answer. The issue's
# own correction says it arrives on BOTH answers and that production, which has
# a processor configured, returns `state: "ready"` — so a client that gated
# cash on `coming_soon` would never show it. `PaymentOrder` does not declare
# the field, so the generated type does not carry it on the 201.
#
# This app therefore reads it by PRESENCE off either answer, through a narrow
# reader that does not depend on the declared type (`readPayAtCounter`). That
# is exactly what the issue asks for, it is correct under both documents, and
# it degrades to "no cash option" if the field never arrives. The contract gap
# is raised on yuvoy-app#29 for the API side to close. (Closed 11 Sep — see
# below.)
#
# ---
#
# 11 Sep 2026. Forward from 64d49ee5481e to 79ce45afd3fb. Additive to
# everything this app reads; nothing removed.
#
# Consumed in this change:
#   - `OperatorSummary.slug`, now REQUIRED (yuvoy-api#152). The operator's name
#     on a reel card and "Who runs this" on a listing link to `/o/{slug}` —
#     the last arrow of reel → listing → operator (app#30). Checked on the live
#     API before building on it: every `/reels` card carries one. A card
#     without it still names the business, with no link: required in the
#     document is a promise about master, not about the deployed API.
#   - `OperatorProfile.about`, `operatingSince`, `languages`, `findThemAt` and
#     `photos` (yuvoy-operator#41). Absent when empty, and each draws nothing
#     when absent — never a heading over a blank. Live on `hc-diving-skl`
#     today: `operatingSince` and `findThemAt`, no words and no photos yet.
#   - `payAtCounter` is now declared on `PaymentOrder` as well as on the
#     `coming_soon` answer, which closes the gap described above. No code
#     changes with it: `readPayAtCounter` already read the field by presence
#     off either answer, and still does.
#
# Found while consuming the above, and fixed in the same change: operator
# pictures are Cloudflare Images (`imagedelivery.net`), which neither the
# enforced CSP's `img-src` nor `next/image`'s `remotePatterns` named. No
# operator had a logo live, so nothing was visibly broken yet; the first
# upload would have been blocked on every card and refused by the optimiser
# on the operator page.
#
# `git ls-remote` was the check for this ref, not `gh api .../commits/master`,
# which served a five-minute-stale SHA twice on 8 Sep and cost a retracted
# issue. And every field above is consumed as OPTIONAL regardless of what
# `required:` says — a pinned contract states what an API WILL send, never what
# it does send today (yuvoy-api#126).
# ---
#
# 13 Sep 2026. Forward from 79ce45afd3fb to d4e03a39ea29, across seven API
# merges (#164, #166, #167, #168, #170, #171, #172, #173). Additive to every
# path this app already calls; the two removals are 409 codes the API has
# stopped RETURNING and deliberately kept in the enum, so nothing narrows.
#
# This bump is a PREREQUISITE, not a feature: the owner walked the app on
# 13 Sep and filed yuvoy-app#32 and #34 through #38, and five of those read
# fields that arrive here. It is pinned in its own change so that the screen
# work that follows shows only screen changes, and so a regression in the
# generated client is bisectable to one commit.
#
# New, and each one is consumed by a named issue:
#   - `GET /catalog/vocabulary` + `VocabularyTerm` + `ActivityTypeTerm`
#     (app#37). The filter chips stop being derived from a 50-item page of
#     `/experiences` and come from the API's own word list. Note the contract's
#     own warning: it publishes the ACTIVE vocabulary, not the populated one,
#     so a chip may legitimately return an empty page.
#   - `GET /reels/{id}` + `ReelItem` (app#36). A shared reel opens the reel.
#     A hidden reel and one that never existed are the same 404 on purpose, so
#     the client must not tell them apart either.
#   - `GET /reels` gains `q`, `destinationKey`, `category`, `activityType`,
#     `bookableOn` (app#37). **The cursor belongs to the filter set it was
#     minted under** — replaying one under different filters is a 400, not a
#     silent re-page. Any client here that keeps a cursor across a chip change
#     is a bug the contract will punish, so the filters are part of the query
#     key and the cursor is dropped with them.
#   - `POST /me/sign-in/request`, `POST /me/sign-in/verify`,
#     `DELETE /me/session` + the `travellerSession` scheme (app#34). This is a
#     SECOND credential beside `statusToken`, not a replacement: recovery still
#     exists for a lost link, where rotating it is the point.
#   - `/me/bookings` now accepts `travellerSession`, and each row gains
#     `reservationId` and a required `statusToken`, plus `reasonCode` and the
#     `pending_request` / `declined` states (app#34). A row for a request the
#     operator has not answered carries an EMPTY `reference`, so anything
#     keyed by reference must key by `reservationId` instead.
#   - `Slot.soldOut`, now required (app#32). The date picker greys out on this
#     and never on `remainingSeats` or `remainingDisplay` — it is the only
#     signal request mode publishes, where seat counts stay withheld. It says
#     nothing about `status` or `bookingCutoffAt`; a closed departure can carry
#     `soldOut: false` and must still be disabled. The fixtures now cover both.
#   - `BookingStatus.payment` (app#29). What a cash booking still owes the
#     operator, beside `confirmed`.
#
# Behaviour that changed under paths already called, which is the dangerous
# half of this bump:
#   - **A cash booking now reads `confirmed`, not `verifying`** (D-034,
#     api#168). `GET /bookings/status` no longer projects `paid_pending_ops`
#     at all; it answers `confirmed` + `final: true` + `payment`. The app's
#     `paid_pending_ops` narrowing therefore stops being reachable through
#     this endpoint. It is NOT deleted here: `GET /me/bookings` still carries
#     the raw state, and a client that had narrowed it correctly should not
#     un-narrow it on a projection change.
#   - **`requestExpiresAt` is the departure's booking cutoff**, often days
#     away, not a two-hour answer clock (api#170). A countdown rendered
#     against it is now wrong by days. Fixed under app#32, not here.
#   - **`request_window_closed` and `request_quota_exhausted` are retired**
#     and never returned. Kept in the `Error.code` enum so exhaustive handling
#     in a deployed client still type-checks, which is the right call and the
#     reason nothing breaks. The panel that renders them goes under app#32.
#   - **`payAtCounter.confirmAt` lost its `/v1` prefix.** It was
#     `/v1/reservations/<id>/cash-booking`, which doubled `/v1` against the API
#     base; it is now relative to the base. This app never used it as a request
#     target — it posts to the typed path and reads `confirmAt` only as the
#     signal that the option exists — so nothing changes here, and Hima
#     confirmed on app#29 that the typed path is the one to keep.
#
# Consumed in THIS change: nothing. The contract, the generated client and the
# fixtures move together; `soldOut` on six fixture slots is the only source
# edit, and it is what the typechecker demanded. Every field above lands with
# the issue that reads it.
#
# `git ls-remote` was the check for this ref, not `gh api .../commits/master`,
# which served a five-minute-stale SHA twice on 8 Sep and cost a retracted
# issue. And every field above is consumed as OPTIONAL regardless of what
# `required:` says — a pinned contract states what an API WILL send, never what
# it does send today (yuvoy-api#126).
#
# ---
#
# 13 Sep 2026, second bump today. Forward from d4e03a39ea29 to 4c9e49f2,
# a large operator-side batch that also carries three traveller features.
# Additive to every path this app already calls; nothing it reads narrows.
#
# The diff was read for removals before pinning, because that is the half that
# breaks a deployed client. There are five, and all five are quoting or
# rewording: `"204": { description: Released, or already was. }` gained quotes,
# so did `whatsapp`'s example, and three prose blocks were rewritten in place.
# Every `required:` change on a RESPONSE widens (`/me/bookings` gains
# `nextCursor`, `BookingStatus` gains `review`, a row gains nine fields), which
# a client reading them as optional cannot notice. No REQUEST gained a required
# field except on the three paths that did not exist before.
#
# New, and each one is consumed by a named issue:
#   - `questions` on `GET /experiences/{slug}` + `ListingQuestion`, `answers`
#     on `POST /reservations` + `BookingAnswer`, `questions` / `answersOpen` on
#     `GET /bookings/status` + `PartyQuestion`, and `POST /bookings/answers`
#     (app#46). A business asks its own questions on a listing.
#   - `GET /bookings/messages`, `POST /bookings/messages`,
#     `POST /bookings/messages/read` + `BookingMessage`,
#     `BookingMessageThread`, `BookingMessageInput`, `BookingMessageReadInput`
#     and `BookingMessageReadReceipt` (app#47). The conversation between the
#     traveller and the business running the trip.
#   - `answers_required`, `answers_closed` and `messages_closed` join the
#     `Error.code` enum. All three are added to `ERROR_CODES` under app#46 and
#     app#47; without them `YuvoyError.code` reads `unknown_error` and
#     `describeError` says "Something went wrong".
#
# Behaviour that changed under paths already called:
#   - **`GET /bookings/cancellation-quote` refunds a moved departure in full**
#     (D-032.3, app#48). If the operator moved the departure while this
#     booking, or the hold it was made from, was on it, the quote is everything
#     paid online whatever tier the snapshot holds, and `note` says why. The
#     sheet showed `note` only when `selfService` was false, so the traveller
#     read "You get everything back." without learning why. Fixed under app#48.
#   - **A partial tier with nothing to refund is now self-service** (D28,
#     app#48). A cash booking cancelled 24 to 48 hours ahead quotes
#     `selfService: true` with `capturedPaise` and `refundPaise` both 0, where
#     it used to route to a person. It reaches the sheet for the first time.
#   - `cancellation.reasonCode` can now read `OPERATOR_MOVED_IT`. It is an open
#     `type: string` with no enum, so this is not a typecheck failure and only
#     `cancellationReason()` stands between it and the fallback sentence "The
#     operator or we called it off." — which is about a booking the traveller
#     cancelled. Mapped under app#48.
#   - **`POST /bookings/review` splits `conflict` into `not_reviewable_yet`,
#     `already_reviewed` and `review_window_closed`**, and `conflict` is no
#     longer returned. `review-form.tsx:60` branches on `conflict` alone to
#     render "Already recorded", so that branch is now dead and all three codes
#     fall through to `describeError`'s default: "Something went wrong ... 
#     trying again often fixes it", with a retry button that fails forever.
#     NOT fixed here, and it is not caused here either — the API has been
#     answering the new codes since 2026-09-13, so the deployed app already
#     reads this way. Found while diffing this bump and raised as its own
#     issue; these three codes are deliberately left out of `ERROR_CODES`
#     so the fix lands with the branch that reads them.
#
# NOT consumed, and deliberately: the operator-side batch this bump is mostly
# made of. `git grep` confirms this repo calls none of those paths.
#
# `git ls-remote` was the check for this ref, not `gh api .../commits/master`,
# which served a five-minute-stale SHA twice on 8 Sep and cost a retracted
# issue. And every field above is consumed as OPTIONAL regardless of what
# `required:` says — a pinned contract states what an API WILL send, never what
# it does send today (yuvoy-api#126).


# ---
#
# 14 Sep 2026. Forward from 4c9e49f28451 to 80668e7f. Eleven new paths,
# thirteen new operations, eight new schemas. This is the batch the owner's
# 14 Sep round reads: the traveller Account, invites, help, and the session
# rule behind yuvoy-app#57.
#
# The removals were read first, because that is the half that breaks a
# deployed client. There are eleven lines and NOT ONE narrows anything:
#   - four are an inline map rewritten as a block so a `description` fits
#     (`expiresAt` on sign-in, `shareUrl` on the trip link). Same type, same
#     `required:`, no generated change beyond a doc comment.
#   - five are prose rewritten in place (`verifyTravellerSignIn`'s 30 days
#     becoming 14 since last use, the `travellerSession` scheme, two
#     `answered: false` sentences that now say answers are deleted 90 days
#     after the trip).
#   - `ReservationContact` loses `required: [name, whatsapp]`. That is a
#     REQUEST body, so dropping a required constraint WIDENS what the API
#     accepts and cannot break a client that keeps sending both. It is the
#     contract catching up with yuvoy-api#183: a signed-in checkout may leave
#     both out. The generated type turns both optional, which relaxes the
#     typechecker rather than tripping it, so the guard here is the issue, not
#     the compiler.
# The `ErrorCode` enum is byte-identical, 40 entries both sides.
#
# New, and each one is consumed by a named issue:
#   - `GET /me` + `PATCH /me` + `TravellerAccount` + `SupportContact`
#     (app#32, app#38, app#57). One read for the whole Account tab: `phone`,
#     `name`, `email`, `interests`, `onboardingRequired`, `memberSince`,
#     `trips`, `reviews` and `support`. `onboardingRequired` is explicitly
#     NEVER a gate on booking, and the first-sign-in screen's skip button is
#     `{"onboarded": true}` on its own.
#   - `session.expiresAt` on `GET /me`, present only under a travellerSession
#     and absent under a statusToken (app#57). The document's own instruction:
#     "Keep a cookie or any stored expiry in step with it rather than with the
#     value sign-in returned." The cookie's Max-Age is therefore re-set from
#     this on every proxied call, not from the sign-in answer.
#   - `GET /me/interest-options` (app#38's first-sign-in screen).
#   - `POST /support/requests` (app#38's help screen).
#   - `POST /bookings/invites`, `GET /bookings/invites`,
#     `DELETE /bookings/invites/{id}`, `GET /invites/{token}`,
#     `POST /invites/{token}/accept`, and the four `/me/invited-trips` paths,
#     with `TripInviteDelivery`, `GuestTripStatus`, `CreatedTripInvite`,
#     `TripGuest`, `TripInvitePreview` and `InvitedTrip` (app#38 items 7 and
#     11). `inviteUrl` is minted as `https://app.yuvoy.in/i/{token}`; it used
#     to be `yuvoy.in/i/...`, which answered 404 (yuvoy-api#188).
#
# Behaviour that changed under a path already called, which is the dangerous
# half of any bump:
#   - **A traveller session now lasts 14 days SINCE LAST USE, not 30 days
#     flat.** Every request it authenticates moves `expiresAt` on. A client
#     that stored the sign-in answer's `expiresAt` once and trusted it will
#     sign a traveller out early, and one that never re-reads it will keep a
#     dead cookie. Both are handled under app#57 by re-setting from
#     `session.expiresAt`.
#   - `ReservationContact` as above. Guest checkout is unchanged and still
#     refused with a `400` naming the field; signed in, `whatsapp` is IGNORED
#     rather than merely optional, so sending it is not a way to override the
#     session's number.
#
# Consumed in THIS change: nothing. The contract, the generated client and the
# fixtures move together, and every field above lands with the issue that
# reads it.
#
# `git ls-remote` was the check for this ref, not `gh api .../commits/master`,
# which served a five-minute-stale SHA twice on 8 Sep and cost a retracted
# issue. And every field above is consumed as OPTIONAL regardless of what
# `required:` says: a pinned contract states what an API WILL send, never what
# it does send today (yuvoy-api#126).
#
# ---------------------------------------------------------------------------
#
# 15 Sep 2026. Forward two commits, 80668e7f -> 0dffd253 (yuvoy-api #190, #191).
# PURELY ADDITIVE to the traveller contract: +28 lines, 0 removed, one enum and
# its prose. Nothing this app already reads changed shape or meaning.
#
# Consumed in this change:
#   - **`bookings[].state` on `GET /me/bookings` is an enum at last** (#190):
#     `pending_request, paid_pending_ops, confirmed, declined, cancelled,
#     completed, no_show`. The values are not new. Only the declaration is, and
#     the declaration is the whole point: `paid_pending_ops` was the state this
#     app had to read to know a cash booking still owes the operator money, and
#     it was absent from every enum in the document. `isCashDueState` therefore
#     took a bare `string`, because TypeScript would reject the comparison
#     against the declared union as having no overlap. That narrowing is now
#     deleted and the comparison is checked (app#29).
#
#     This cost two live defects before it was declared, both on the screen of
#     somebody who had just committed money: an error boundary, and a false
#     "Paid" shown to a traveller about to be asked for the notes. A state
#     outside the enum does not fail to compile; it silently stops matching.
#     yuvoy-api now has a test asserting this list against the database and the
#     contract in both directions, so a new state cannot appear undeclared.
#
# Not consumed, and deliberately:
#   - `getBookingStatus`'s own `BookingStatus.state` enum is UNCHANGED and
#     still has no `paid_pending_ops`. Two endpoints, two shapes, on purpose: a
#     cash booking reads `confirmed` there with a `payment` block (D-034). The
#     app reads `payment` on that screen and the enum here, and the two must not
#     be collapsed into one helper.
#   - #191 is operator media cover selection. It touches
#     `contracts/operator-openapi.yaml`, which this app neither generates from
#     nor drift-checks, so the vendored copy is left at the ref it was last
#     pulled at (0f31f3f, 31 Aug). It is dead weight and should be deleted
#     rather than maintained; not done here to keep this change to app#29.
#
# Checked with `git ls-remote`, per the note above.
#
# ---------------------------------------------------------------------------
#
# 21 Sep 2026. Forward from 0dffd253 to 6caa7289, across yuvoy-api #194,
# #198, #199, #208, #209 and #210. Additive to every path this app already
# calls.
#
# The removals were read first. There are four lines and none of them narrows
# anything:
#   - two are the `/reels` cursor sentence, rewrapped around "the ranges
#     included". Same rule, now naming the four new range filters.
#   - two are `required:` lists on RESPONSES that each gained one entry:
#     `unreadCount` on a `/me/bookings` row and `admitted` on
#     `TravellerAccount`. A response widening its required set cannot break a
#     client that reads the new field as optional, which this app does.
# No REQUEST gained a required field on a path that already existed.
#
# New, and each one is consumed by a named issue:
#   - `GET /me/saved`, `GET /me/saved/ids`, `POST /me/saved`,
#     `DELETE /me/saved/{experienceId}`, `POST /me/saved/adopt` and
#     `SavedIds`, `SavedExperience`, `SavedPage` (yuvoy-api#192). Saves move
#     onto the account. Session only: a booking's status token can neither
#     read nor write them.
#   - `price_moved` joins `ErrorCode` (yuvoy-api#193). This app declared it
#     locally before the enum did; the note saying the enum lacked it goes.
#   - `unreadCount` on each `/me/bookings` row (yuvoy-api#207).
#   - `minDurationMinutes`, `maxDurationMinutes`, `minPriceMinor` and
#     `maxPriceMinor` on `GET /reels` (yuvoy-api#197). Setting a price bound
#     leaves `per_group` listings out, by the owner's decision.
#   - `GET /support/requests`, `GET /support/requests/{reference}`,
#     `SupportRequest` and `SupportRequestPage` (yuvoy-api#196).
#   - `POST /me/invite-codes/redeem`, `admitted` on `GET /me`,
#     `403 invite_required` on `POST /reservations`, and four new `ErrorCode`
#     entries (yuvoy-api#195). The four are added to `ERROR_CODES` in this
#     change, each with a sentence, because `pnpm qa` refuses a declared code
#     the client cannot name. They are string-keyed, so they are inert until
#     the API sends them.
#
# NOT LIVE when this was pinned, and every consumer is built for that. From
# 20 Sep GitHub refused to start yuvoy-api's deploy jobs ("recent account
# payments have failed or your spending limit needs to be increased"), so
# #208, #209 and #210 were merged and not deployed: on 21 Sep
# `POST /me/invite-codes/redeem` answered 404, `GET /support/requests` 405,
# and the four `/reels` ranges were ignored rather than refused.
# `GET /me/saved/ids` (deployed 16 Sep) answered 401 signed out, as it should.
# So every field above is read as OPTIONAL and every new route degrades to what
# this app did before the route existed.
#
# Checked with `git ls-remote`, per the note above.
#
# ---------------------------------------------------------------------------
#
# 22 Sep 2026. Forward from 6caa7289 to e7291e35, across yuvoy-api #211 to
# #221. Only three of those eleven merges touch this document (#213, #214 and
# #215); the rest are operator-side and live in `operator-openapi.yaml`.
# Additive to every path this app already calls.
#
# The removals were read first. There are sixteen lines and every one is
# prose rewritten in place:
#   - ten are the `/reels` ordering and cursor paragraphs, rewritten around
#     "shuffled per visit" (#213). The sentence "Unfiltered cursors are
#     unchanged from before filters existed" is the one that matters, and it
#     is gone because it stopped being true: see below.
#   - four are the `/reels` filter paragraph: "stable between requests" became
#     "stable within a visit".
#   - two are descriptions of `holdExpiresAt` on the reservation and
#     `expiresAt` on `PaymentOrder`, each extended rather than changed (#215).
# The `ErrorCode` enum is byte-identical, so `ERROR_CODES` needs nothing. No
# `required:` list moved and no type changed on any existing path.
#
# New, and each one is consumed by a named issue:
#   - `POST /reel-views`, `ReelViewEvent` and the `202` batch receipt with
#     `droppedEvents` (yuvoy-api#214, consumed by yuvoy-app#96). Anonymous
#     unless a session is sent, and by the owner's ruling this app never sends
#     one: views go to the API directly, with no Authorization header, and
#     only while analytics consent is granted.
#
# Behaviour that changed under paths already called, which is the dangerous
# half of this bump:
#   - **`GET /reels` is shuffled per visit** (#213). A request without a
#     cursor starts a new visit in a new order, so a background refetch of the
#     feed would reshuffle it under a traveller's thumb. yuvoy-app#96 makes the
#     feed and the search grid one stable visit each.
#   - **A cursor minted before 2026-09-21 is a `400 invalid_input`, once.**
#     Every phone that had the feed open across the deploy holds one. The
#     issue asks for a fresh first page rather than an error, and that is
#     built under yuvoy-app#96, de-duplicated so nothing already seen repeats.
#   - **An accepted request holds for twelve hours** (#215, api#203), capped
#     at the departure's booking cutoff. `holdExpiresAt` and
#     `PaymentOrder.expiresAt` can be on the next day, and the contract now
#     says to show such a deadline as a time with the day rather than as a
#     ticking count of minutes. yuvoy-app#97.
#
# Consumed in THIS change: nothing. The contract and the generated client move
# together, and every item above lands with the issue that reads it.
#
# Live, checked on 22 Sep rather than assumed: `POST /reel-views` with an
# empty batch answers `400` naming `events`, `GET /support/requests` answers
# `401` signed out, and `/reels` refuses a minimum duration above its maximum
# with a `400` naming both. The routes the 21 Sep record found undeployed are
# deployed now.
#
# Checked with `git ls-remote`, per the note above.

ref=e7291e3543072083cff851714b97fabe3cd4f111
branch=master
pinned_on=2026-09-22
